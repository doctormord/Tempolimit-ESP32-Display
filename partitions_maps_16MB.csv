# Partitionstabelle fuer die Tempolimit-Anzeige, 16 MB Flash.
#
# Ziel: moeglichst viel Platz fuer Kartendaten. Gegenueber
# large_spiffs_16MB.csv (4,5 MiB je App-Slot, 6,875 MiB FS) schrumpft der
# App-Bereich und das Dateisystem waechst auf 12,875 MiB.
#
# Ein einziger "factory"-Slot statt app0/app1+otadata: OTA wird im Projekt
# nicht genutzt, ein zweiter (nie beschriebener) App-Slot war also reine
# Verschwendung. Seit webupdate.h/.cpp (Kartenupdate per Access Point,
# WiFi+WebServer) braucht die Firmware selbst deutlich mehr Platz - gemessen
# 1,43 MiB, mit den alten 1,5 MiB je Slot waeren das 95,1 % und praktisch
# keine Reserve mehr. Der eine factory-Slot fasst dafuer 3 MiB, den doppelten
# Platzbedarf mit Reserve.
#
# App-Partitionen muessen auf 0x10000 ausgerichtet sein, deshalb bleibt der
# Offset bei 0x10000 wie zuvor bei app0 - das freigewordene otadata (0x2000)
# geht stattdessen an nvs. Bewusst so aufgeteilt, dass sich am Ende des
# App-Bereichs (0x310000) nichts aendert: die FS-Partition behaelt Offset und
# Groesse von vorher. Wer schon eine Karte hochgeladen hat, muss nach dem
# Umstieg auf diese Tabelle nicht neu "uploadfs" laufen lassen - nur die
# Firmware selbst neu flashen.
#
# Die FS-Partition heisst "spiffs" und hat Subtype spiffs. Nicht umbenennen:
# LittleFS.begin() mountet per Default genau dieses Label, und
# "pio run -t uploadfs" sucht die letzte Data-Partition mit diesem Subtype.
#
# Name,   Type, SubType,  Offset,   Size,     Flags
nvs,      data, nvs,      0x9000,   0x7000,
factory,  app,  factory,  0x10000,  0x300000,
spiffs,   data, spiffs,   0x310000, 0xCE0000,
coredump, data, coredump, 0xFF0000, 0x10000,
